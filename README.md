# Dotfiles

## To Use

1. Clone the repo into the home directory.

   ```console
   git clone https://github.com/Nilsojunior/dotfiles ~
   ```

2. Use GNU stow.

   `~/.config`

   ```console
   stow .
   ```

   `.zshrc`

   ```console
   stow -v -t zsh/
   ```
