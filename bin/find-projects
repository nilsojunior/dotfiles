#!/usr/bin/env bash

dirs=(
  "$HOME/dotfiles"
  "$HOME/notes"
  "$HOME/personal"
  "$HOME/faculdade"
)

dirs+=$(find "${dirs[@]}" -mindepth 1 -maxdepth 1 -type d -not -path '*/.*')

dirs=$(echo "$dirs" | tr ' ' '\n')

selected=$(echo "$dirs" | fzf)

if [ -n "$selected" ]; then
  cd "$selected"
fi
